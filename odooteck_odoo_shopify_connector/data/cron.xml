<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Scheduled Action: Auto Import Shopify Orders -->
        <record id="ir_cron_shopify_sync_orders" model="ir.cron">
            <field name="name">Shopify Connector: Auto-Sync Orders</field>
            <field name="model_id" ref="model_shopify_instance"/>
            <field name="state">code</field>
            <field name="code">model.cron_sync_orders()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">hours</field>
            <field name="active">False</field>
        </record>

        <!-- Scheduled Action: Auto Sync Inventory to Shopify -->
        <record id="ir_cron_shopify_sync_inventory" model="ir.cron">
            <field name="name">Shopify Connector: Auto-Sync Inventory</field>
            <field name="model_id" ref="model_shopify_instance"/>
            <field name="state">code</field>
            <field name="code">model.cron_sync_inventory()</field>
            <field name="interval_number">30</field>
            <field name="interval_type">minutes</field>
            <field name="active">False</field>
        </record>
    </data>
</odoo>
