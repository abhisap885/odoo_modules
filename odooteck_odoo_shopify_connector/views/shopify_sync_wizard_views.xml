<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Wizard Form View -->
        <record id="view_shopify_sync_wizard_form" model="ir.ui.view">
            <field name="name">shopify.sync.wizard.form</field>
            <field name="model">shopify.sync.wizard</field>
            <field name="arch" type="xml">
                <form string="Shopify Synchronization">
                    <sheet>
                        <group>
                            <group>
                                <field name="instance_id" widget="selection"/>
                                <field name="operation_type" widget="radio"/>
                                <field name="entity_type"/>
                            </group>
                            <group>
                                <field name="record_limit" invisible="operation_type == 'export'"/>
                                <field name="date_from" invisible="operation_type == 'export'"/>
                                <field name="auto_process_feed" invisible="operation_type == 'export'"/>
                            </group>
                        </group>
                    </sheet>
                    <footer>
                        <button name="action_execute_sync" string="Execute Synchronization" type="object" class="btn-primary"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <!-- Wizard Window Action -->
        <record id="action_shopify_sync_wizard" model="ir.actions.act_window">
            <field name="name">Shopify Sync Operations</field>
            <field name="res_model">shopify.sync.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>
    </data>
</odoo>
